/**
@page heat_equation Уравнение теплопроводности в П-образной области
@tableofcontents

@section heat_equation_desc Описание задачи
Решается двумерное нестационарное уравнение теплопроводности

∂u/∂t = Δu

в П-образной области: квадрат [0,3]x[0,3] без правого среднего
подквадрата [2,3]x[1,2].

Граничные условия:
на отрезке от точки (0,0) до точки (3,0) u = 0;  
на отрезке от точки (0,0) до точки (0,3) u = y; 
на отрезке от точки (0,3) до точки (3,3) ∂u/∂y = 0;
на отрезке от точки (3,3) до точки (3,2) u = 4; 
на отрезке от точки (3,2) до точки (2,2) u = 1+x; 
на отрезке от точки (2,2) до точки (2,1) ∂u/∂x = 0; 
на отрезке от точки (2,1) до точки (3,1) u = 2-x; 
на отрезке от точки (3,1) до точки (3,0) u = -y.
Начальное условие: u(x,y,0)=0 внутри области (опционально можно задать функцией).

@section heat_equation_numerics Численный метод
Используется явная разностная схема на равномерной сетке с шагом
h=1/M. Узлы: (x_j, y_i), где x_j = jh, y_i = ih,
i,j=0,...,2M.

Внутренние точки:

(u_{i,j}^{n+1} - u_{i,j}^n)/(tau) =
(u_{i-1,j}^n - 2u_{i,j}^n + u_{i+1,j}^n)/(h^2) +
(u_{i,j-1}^n - 2u_{i,j}^n + u_{i,j+1}^n)/(h^2).


Условия Неймана аппроксимируются копированием соседнего значения:
u_{i,0} = u_{i,1} и u_{M,j} = u_{M+1,j}.

Условие устойчивости: tau <= h^2/4.

Параллелизм: вычисление внутренних узлов разбивается на блоки строк и
выполняется в отдельных потоках через std::thread.

@section heat_equation_params Параметры запуска
Входной JSON для серверного метода HeatEquation:
- M (int) -- число разбиений на единицу длины,
- tau (double)-- шаг по времени,
- finishTime (double) -- конечное время моделирования,
- exportPeriod (double) -- период сохранения данных.

@section heat_equation_output Выходной формат
Результат сохраняется в JSON-массив data, каждый элемент которого имеет
поля:
- time -- момент времени,
- data -> grid -- двумерный массив размера \f$(3M+1)\times(3M+1)\f$,
  где внутренние узлы содержат температуру, а точки вне области -- null.

Пример визуализации: см. класс HeatEquationPlotter.

*/